{% set name = "plantcv" %}
{% set version = "4.10" %}
{% set python_min = "3.9" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 283c16c348b4f40855de855c6860d6138307e65f39dbf8a0e8d0f115df5d20a8
  patches:
    - 0001-Patch-pyproject.patch

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv --no-build-isolation

requirements:
  host:
    - setuptools
    - setuptools_scm
    - pip
    - python {{ python_min }}
  run:
    - matplotlib-base >=1.5
    - numpy >=2
    - opencv
    - pandas
    - python >={{ python_min }}
    - python-dateutil
    - scikit-image >=0.19
    - scikit-learn
    - scipy
    - dask
    - dask-jobqueue
    - statsmodels
    - xarray >=2022.11.0
    - altair
    - vl-convert-python
    - nd2

test:
  imports:
    - plantcv
    - plantcv.learn
    - plantcv.parallel
    - plantcv.utils
    - plantcv.plantcv
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip
    - libopencv *=headless*

about:
  home: https://plantcv.org
  dev_url: https://github.com/danforthcenter/plantcv
  doc_url: https://plantcv.readthedocs.io
  summary: An image processing package for plant phenotyping.
  license: MPL-2.0
  license_family: MOZILLA
  license_file: LICENSE

extra:
  recipe-maintainers:
    - nfahlgren
